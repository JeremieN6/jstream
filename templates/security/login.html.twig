{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
<section class="sign-in-page">
    <div class="container">
       <div class="row justify-content-center align-items-center height-self-center">
          <div class="col-lg-5 col-md-12 align-self-center">
             <div class="sign-user_card ">                    
                <div class="sign-in-page-data">
                   <div class="sign-in-from w-100 m-auto">
                      <h3 class="mb-3 text-center">Connexion</h3>
                      <form method="post">
                        {% if error %}
                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}
                
                    {% if app.user %}
                        <div class="mb-3">
                            Vous êtes déjà connecté avec le compte {{ app.user.userIdentifier }} • <a href="{{ path('app_logout') }}">Se Déconnecter ?</a>
                        </div>
                    {% endif %}
                         <div class="form-group">                                 
                            <label for="inputEmail">Email</label>
                            <input type="email" value="{{ last_username }}" name="email" placeholder="Email" id="exampleInputEmail1" class="form-control mb-0" autocomplete="email" required autofocus>
                            <!-- <input type="email" class="form-control mb-0" id="exampleInputEmail1" placeholder="Enter email" autocomplete="off" required=""> -->
                         </div>
                         <div class="form-group">                                 
                            <label for="inputPassword">Password</label>
                            <input type="password" name="password" id="exampleInputPassword2" class="form-control mb-0" autocomplete="current-password" required>
                            <!-- <input type="password" class="form-control mb-0" id="exampleInputPassword2" placeholder="Password" required=""> -->
                         </div>
                         
                            <div class="sign-info">
                                <button type="submit" class="btn btn-hover"> Connexion </button>
                               <!-- <button type="submit" class="btn btn-hover">Connexion</button> -->
                               <div class="custom-control custom-checkbox d-inline-block">
                                  {# <input type="checkbox" class="custom-control-input" id="customCheck">
                                  <label class="custom-control-label" for="customCheck">Se souvenir de moi</label> #}
                                  <input type="hidden" class="custom-control-input" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                               </div>                                
                            </div>                                    
                         
                      </form>
                   </div>
                </div>
                <div class="mt-3">
                   <div class="d-flex justify-content-center links">
                      Pas encore de compte 😯 ? <a href="{{ path("app_register")}}" class="text-primary ml-2">S'Inscrire</a>
                   </div>
                   <div class="d-flex justify-content-center links">
                      <a href="{{ path("forgetten_password")}}" class="f-link">Vous avez oublié votre mot de passe 🙄?</a>
                   </div>
                </div>
             </div>
          </div>
       </div>
    </div>
 </section>
{% endblock %}
