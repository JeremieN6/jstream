{% extends 'base.html.twig' %}

{% block body %}

<section class="m-profile manage-p">
    <div class="container h-100">
        <div class="row align-items-center justify-content-center h-100">
            <div class="col-lg-10">
                <div class="sign-user_card">
                    {{ form_start(userForm) }}
                    <div class="row">
                        <div class="col-lg-2">
                            <div class="upload_profile d-inline-block">
                                {# {{ form_row(userForm.imageFile,
                                    {'attr':{'class':'profile-pic rounded-circle img-fluid'}}) }} #}
                            <img src="images/user/user.jpg" class="profile-pic rounded-circle img-fluid" alt="user">
                            {# <div class="p-image">
                                <i class="ri-pencil-line upload-button"></i>
                                <input class="file-upload" type="file" accept="image/*">
                            </div> #}
                            </div>
                        </div>
                        <div class="col-lg-10 device-margin">
                            <div class="profile-from">
                                <h4 class="mb-3">Mon Profil</h4>
                                {# <form class="mt-4" action="home.html"> #}
                                    {# <div class="form-group">
                                        {{ form_row(userForm.email,
                                            {'attr':{'class':'form-control mb-0'}}) }}
                                    </div> #}
                                    <div class="form-group">
                                        {{ form_row(userForm.pseudo,
                                            {'attr':{'class':'form-control mb-0'}}) }}
                                        {# <label>Pseudo</label> #}
                                        {# <input type="text" class="form-control mb-0" id="exampleInputl2"
                                            placeholder="Enter un nouveau pseudo" autocomplete="off" required> #}
                                    </div>
                                    <div class="form-group">
                                        {{ form_row(userForm.description,
                                            {'attr':{'class':'form-control mb-0'}}) }}
                                    </div>
                                    <div class="form-group">
                                        {{ form_row(userForm.birthday) }}
                                    </div>
                                        {# <div class="form-group">
                                            <label>Date de Naissance</label>
                                            <input type="date" class="form-control date-input basicFlatpickr mb-0" placeholder="Entrer votre date de naissance" id="exampleInputPassword2"
                                            required>
                                        </div> #}
                                    <div class="form-group d-flex flex-md-row flex-column">
                                        <div class="iq-custom-select d-inline-block manage-gen">
                                            {{ form_row(userForm.genre,
                                                {'attr':{'class':'form-control pro-dropdown'}}) }}
                                            {# <select name="cars" class="form-control pro-dropdown">
                                                <option value="female">Femme</option>
                                                <option value="male">Homme</option>
                                                <option value="male">Non binaire</option>
                                            </select> #}
                                        </div>
                                        <div class="iq-custom-select d-inline-block lang-dropdown manage-dd">
                                            {{ form_row(userForm.favLangue,
                                                {'attr':{'class':'form-control pro-dropdown'}}) }}
                                            {# <select name="cars" class="form-control pro-dropdown" id="lang"
                                                multiple="multiple">
                                                <option value="english">English</option>
                                                <option value="français">Français</option>
                                                <option value="espagnol">Espagnol</option>
                                            </select> #}
                                        </div>
                                    </div>
                                    <div class="d-flex">
                                        <button type="submit" class="btn btn-hover">Valider</button>
                                        <a href="{{ path('parametres') }}" class="btn btn-link">Annuler</a>
                                    </div>
                                {# </form> #}
                                {{ form_end(userForm) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{% endblock %}